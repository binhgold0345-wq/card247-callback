services:
  - type: worker
    name: tb-tool-worker
    runtime: python
    region: singapore  # Chọn server gần VN nhất
    plan: starter      # BẮT BUỘC dùng gói Starter (trả phí) để lưu session lâu dài
    buildCommand: pip install -r requirements.txt
    startCommand: python nbxx88.py
